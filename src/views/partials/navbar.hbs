<nav class="navbar is-fixed-top is-light" role="navigation" aria-label="Navegação principal">
  <div class="container">
    <div class="navbar-brand">
      <a
        class="navbar-item has-text-weight-semibold navbar-brand-link"
        href="{{#if isHome}}#home{{else}}/#home{{/if}}"
        aria-label="Ir para o início"
      >
        <img src="/public/img/museu-logo.webp" alt="Logo Museu do Vinho" class="navbar-brand-logo" loading="eager" />
        <span class="navbar-brand-stack">
          <span class="navbar-brand-text">Museu do Vinho</span>
          <span class="navbar-brand-highlight">Mario de Pellegrin</span>
        </span>
      </a>
      <button
        class="button is-ghost navbar-item theme-toggle theme-toggle--mobile"
        data-theme-toggle
        type="button"
        title="Alternar tema claro/escuro"
        aria-label="Alternar tema claro/escuro"
      >
        <span class="icon"><i class="fa-solid fa-moon" aria-hidden="true"></i></span>
      </button>
      <button
        class="navbar-burger"
        id="navbar-burger"
        type="button"
        aria-label="Abrir menu"
        aria-expanded="false"
        aria-controls="main-navbar-menu"
      >
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </button>
    </div>
    <div class="navbar-menu" id="main-navbar-menu">
      <div class="navbar-end">
        <a class="navbar-item {{#if isHome}}is-active{{/if}}" href="{{#if isHome}}#home{{else}}/#home{{/if}}">Início</a>
        <a class="navbar-item" href="{{#if isHome}}#institucional{{else}}/#institucional{{/if}}">Sobre</a>
        <a class="navbar-item" href="{{#if isHome}}#timeline{{else}}/#timeline{{/if}}">Linha do tempo</a>
        <a class="navbar-item" href="{{#if isHome}}#contato-link{{else}}/#contato-link{{/if}}">Contato</a>
        <a class="navbar-item {{#if isAcervo}}is-active{{/if}}" href="/acervo">Acervo</a>
        <button
          class="button is-ghost navbar-item theme-toggle theme-toggle--desktop"
          data-theme-toggle
          type="button"
          title="Alternar tema claro/escuro"
          aria-label="Alternar tema claro/escuro"
        >
          <span class="icon"><i class="fa-solid fa-moon" aria-hidden="true"></i></span>
        </button>
      </div>
    </div>
  </div>
</nav>
